node_modules/

release/*.zip
*.zip

# Secrets (never commit)
.env
.env.local
.env.*.local
feed-server/.env
feed-server/.env.local

# Local signing / packaging (do not publish)
*.pem
*.crx

# License / trial storage (local server only)
feed-server/licenses.json
feed-server/licenses.json.tmp
feed-server/trials.json
feed-server/activations.json

.DS_Store
.vercel
.env*.local
